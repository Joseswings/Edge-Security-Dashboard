# El navegador consulta /api; Vite reenvía las solicitudes a Express.
API_PROXY_TARGET=http://127.0.0.1:3001
# Opcional: URL pública de una API con CORS configurado o un proxy común.
VITE_API_BASE_URL=/api
